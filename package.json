{
  "name": "design-patterns",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "test": "bun test --watch"
  },
  "devDependencies": {
    "bun-types": "^1.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}